---
- name: mongodb configuration
  hosts: mongodb
  become: true
  tasks:
    - name: install mongodb server
      ansible.buitin.dnf:
        name: mongodb-org
        state: present
    - name: start & enable mongod
      ansible.buitin.service:
        name: mongod
        state: started
        enabled: yes

